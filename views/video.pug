extends layout

block content
  h1 Convert Video to MP4
  form(action="/videoconvert" method="POST" enctype="multipart/form-data")
    p
      input(type="file" name="videoHere" accept="video/*" required="required")
    input(type="submit")
