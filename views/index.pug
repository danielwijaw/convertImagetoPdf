extends layout

block content
  h1 Convert Image to PDF
  form(action="/convert" method="POST" enctype="multipart/form-data")
    p
      input(type="file" name="imageHere" accept="image/*" required="required")
    input(type="submit")
